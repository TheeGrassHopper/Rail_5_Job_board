= simple_form_for(@job, html: {class: 'form-horisontal'}) do |f|
	= f.input :title, label: 'Job Title'
	= f.input :description, label: 'Job Dscription'
	= f.input :company, label: 'Your Company'
	= f.input :url, label: 'Link to Job'
	%br/
	= f.button :submit